# SECURITY: the RSA private key that used to live in this file was removed
# because it had been committed to version control (git history) and must be
# treated as compromised. It needs to be ROTATED (revoke/reissue a new
# key/cert pair) — do not restore the old material.
#
# This file is intentionally NOT a valid PEM anymore. Do not load it as-is.
# For local/dev HTTPS, generate your own key/cert pair and provide it via an
# environment-configurable, gitignored path (see TLS_KEY_PATH / TLS_CERT_PATH
# in server.js and config/env/*.js) instead of committing key material here,
# e.g.:
#   openssl req -x509 -newkey rsa:2048 -nodes \
#     -keyout artifacts/cert/server.key -out artifacts/cert/server.crt \
#     -days 365 -subj "/CN=localhost"
# and keep the generated files out of git (see .gitignore).
