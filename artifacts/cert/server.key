# SECRET REMOVED — rotate this credential immediately; it exists in git history and must be revoked.
# PLACEHOLDER: Do not commit real private keys to source control.
# Generate a self-signed certificate locally:
#   openssl req -x509 -newkey rsa:2048 -keyout server.key -out server.crt -days 365 -nodes
# Or load the key path from an environment variable / secrets manager at runtime.
