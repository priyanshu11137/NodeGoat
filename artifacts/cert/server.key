SECRET REMOVED — rotate this credential immediately.
This RSA private key has been removed from version control because it was exposed in git history.
The original key is compromised and must be revoked and reissued.
Load the private key from an environment variable or a secrets manager at runtime, for example:
  process.env.SSL_PRIVATE_KEY
Do NOT commit real private key material to this file.
