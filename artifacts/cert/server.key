# SECRET REMOVED -- rotate this credential immediately.
# The RSA private key that was stored here has been removed because it was
# committed to version control and must be considered compromised.
#
# To provide a replacement key, do ONE of the following:
#   1. Set the environment variable TLS_PRIVATE_KEY_PATH to point to the real
#      key file on disk (generated outside of source control).
#   2. Place a new key at this path, ensuring it is listed in .gitignore so it
#      is never committed.
#
# Generate a new key (example):
#   openssl genrsa -out artifacts/cert/server.key 2048
